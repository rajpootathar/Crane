/target
.DS_Store
Cargo.lock
/icons/crane.iconset/
# Local signing env (DEVELOPER_ID, NOTARY_PROFILE). Never committed.
.env.local

.idea/

# Local third-party checkouts kept on disk for research / reference.
# Their own repos own the source — never committed into Crane.
/alacritty-fork
/warp-terminal
/warp-vte
/vte
